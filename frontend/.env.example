# Copy to frontend/.env.local for Sign in / saved library (optional).
# Must match your Supabase project (same values as root .env SUPABASE_*).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Production (e.g. Netlify): HTTPS origin of Flask API (Cloudflare tunnel). Omit for local dev (Vite proxies /api).
# VITE_API_BASE_URL=https://api.example.com
